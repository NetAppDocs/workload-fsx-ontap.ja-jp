---
sidebar: sidebar 
permalink: set-volume-latency-thresholds.html 
keywords: latency, latency thresholds, alerts, volume latency, read latency, write latency 
summary: ボリュームのレイテンシしきい値を設定すると、読み取りまたは書き込みのレイテンシが指定された値を超えた場合にアラートが送信されます。 
---
= NetApp Workload Factoryでボリューム遅延しきい値を設定する
:allow-uri-read: 
:icons: font
:imagesdir: ./media/


[role="lead"]
ボリュームのレイテンシしきい値を設定すると、読み取りまたは書き込みのレイテンシが指定された値を超えた場合にアラートが送信されます。

.タスクの内容
ボリュームごとにレイテンシのしきい値を設定することで、パフォーマンスを監視し、レイテンシの値が定義した制限を超えた場合にアラートを受け取ることができます。Workload Factoryは、読み取りレイテンシと書き込みレイテンシの両方を監視し、いずれかのしきい値を超えた場合にアラートを生成します。

レイテンシのしきい値を設定すると、Workload Factory は以下の項目を監視します：

* *読み取り遅延*：読み取り操作が指定されたしきい値（ミリ秒単位）を超えた場合にアラートをトリガーします。
* *書き込み遅延*：書き込み操作が指定されたしきい値（ミリ秒単位）を超えた場合にアラートをトリガーします。


レイテンシ監視では、以下のデフォルトパラメータが使用されます：

* *IOPS しきい値*：100 IOPS（読み取りおよび書き込み）
* *時間範囲*：10分


指定された時間範囲内で、レイテンシのしきい値を超え、かつIOPSのしきい値に達した場合にアラートが生成されます。


NOTE: リンク接続がなくても、遅延イベントを表示したり、アラートを受信したりできます。ただし、イベントを分析し、AI を活用した洞察や推奨事項を受け取るには、link:https://docs.netapp.com/us-en/workload-fsx-ontap/create-link.html["リンクを使用してファイルシステムに接続する"]。

.手順
. いずれかを使用してログインしlink:https://docs.netapp.com/us-en/workload-setup-admin/console-experiences.html["コンソールエクスペリエンス"^]ます。
. メニューを選択 image:workloads-menu-icon.png["ハンバーガー メニュー アイコンは、ストレージ、eda、ai、データベース、vmware、管理などのワークロードに移動するために使用されます。"] 次に*ストレージ*を選択します。
. ストレージメニューから「ボリュームエクスプローラー」を選択します。
. レイテンシーしきい値を設定するボリュームのアクションメニューを選択します。
. 「アラートアクション」を選択し、「遅延しきい値の設定」を選択します。
. 「遅延しきい値の設定」ダイアログで、以下を設定します：
+
.. *読み取りレイテンシ*：読み取り操作のしきい値をミリ秒（ms）単位で入力してください。
.. *書き込みレイテンシ*：書き込み操作のしきい値をミリ秒（ms）単位で入力します。


. 遅延しきい値を適用するには、*保存*を選択してください。


.結果
Workload Factoryはボリュームを監視し、読み取りまたは書き込みのレイテンシが設定されたしきい値を超えた場合にアラートを生成します。レイテンシイベントはlink:analyze-events.html["分析画面"]から確認できます。

.関連情報
* link:analyze-events.html["NetApp Workload FactoryでFSx for ONTAPイベントを分析する"]
* link:adjust-volume-capacity.html["Workload Factoryでボリューム容量を調整する"]

